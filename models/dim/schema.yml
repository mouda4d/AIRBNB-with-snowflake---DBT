version: 2

models:
  - name: dim_hosts
    columns:
      - name: host_id
        tests:
          - unique
          - not_null

  - name: dim_listings
    columns:
      - name: listing_id
        tests:
          - unique
          - not_null
